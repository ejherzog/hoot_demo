<!DOCTYPE html>
<html>
  <%- include('./partials/_head'); %>
  <body>
    <%- include('./partials/_nav'); %>
    <div class="container mt-2">
      <hr>
      <section>
        <table class="table table-sm table-striped">
          <tr>
            <th>Variable</th>
            <th>Type</th>
            <th>Category</th>
            <th>Color</th>
            <th>Fave</th>
            <th>Edit</th>
          </tr>
          <% variables.forEach(function(v) { %>
            <tr>
              <td><%= v.variable %></td>
              <td><%= v.type %><%= v.type == 'scalar' ? ` (${v.subtype})` : '' %></td>
              <td><%= v.category %></td>
              <td style="color:<%= v.color %>">&#9632;</td>
              <td><%= v.shortcut == '1' ? '✅' : '' %></td>
              <td><a href="/edit/variable/<%= v._id %>">Edit</a></td>
            </tr>
          <% }) %>
        </table>
      </section>
      <hr>
      <section>
        <table class="table table-sm table-striped">
          <tr>
            <th>Variable</th>
            <th>Data</th>
            <th>Last Updated</th>
          </tr>
          <% records.forEach(function(r) { %>
            <tr>
              <td><%= r.variable %></td>
              <td><%= r.data %></td>
              <td><%= r.moment %></td>
            </tr>
          <% }) %>
        </table> 
      </section>
      <hr>
      <%- include('./partials/_footer'); %>
    </div>
  </body>
</html>